generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model FakeUser {
  id       Int    @id @default(autoincrement())
  uuid     String @unique
  email    String @unique
  name     String
  password String
}
